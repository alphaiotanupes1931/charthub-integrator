# Correct TradeLocker social-login guidance

## What will change
- Remove the incorrect instruction telling Google or Apple users to set a TradeLocker password.
- Clearly state that TradeLocker’s public connection currently requires email, password, and server details.
- Explain that a social-only TradeLocker account cannot be connected unless the user’s broker or TradeLocker supplies separate API-compatible credentials.
- Keep the direct TradeLocker link for contacting support or checking account options, and point users toward another supported broker when credentials are unavailable.

## Technical details
- Update only the TradeLocker connection panel wording and available actions.
- Keep the existing password-based connection working for accounts that already have API-compatible credentials.
- Verify the broker page displays the corrected guidance without layout or type errors.
